#include <unistd.h>

void	ft_putchar(char c)
{
	write(1, &c, 1);
}

void	print_f
{
	int empty;

	empty = 1;
	if (empty < length - 1)
	{
		
		


void	set(int length, int height)
{ 
	if (length > 0 && y > height)
	{
		print_f;
	}
}

int	main()
{
	set(5, 5);
	return (0);
}
